{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "layout_pack.json",
  "title": "LayoutPack Command",
  "description": "Create A3 landscape Layout pages. Each page has one parallel Detail of the greyscale HiddenLineDrawing on S-DRAW and a title block at the bottom-right. The plan drawing is a cut 1200 mm above the floor.",
  "type": "object",
  "properties": {
    "paper": {
      "type": "string",
      "enum": ["A3"],
      "default": "A3"
    },
    "views": {
      "type": "array",
      "description": "Views to lay out, in order. Default plan, north, east, south, west.",
      "items": {
        "type": "string",
        "enum": ["plan", "north", "east", "south", "west"]
      },
      "default": ["plan", "north", "east", "south", "west"]
    },
    "scale": {
      "type": "integer",
      "minimum": 1,
      "default": 100,
      "description": "Requested detail scale denominator. 100 means 1:100. Bumped if the drawing does not fit the detail."
    },
    "replace": {
      "type": "boolean",
      "default": true,
      "description": "Delete existing Forsk Layout pages for those views before creating them."
    },
    "include_existing": {
      "type": "boolean",
      "default": true,
      "description": "Include X-EXIST in the greyscale drawing."
    }
  },
  "additionalProperties": false
}
