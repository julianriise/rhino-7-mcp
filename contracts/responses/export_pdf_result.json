{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "export_pdf_result.json",
  "title": "ExportPdfResult",
  "description": "PDF written by FilePdf, or a refuse message when the path, layouts, or detail ink is missing. Rhino 7 Mac writes raster pages.",
  "type": "object",
  "properties": {
    "path": { "type": "string" },
    "count": { "type": "integer" },
    "pages": {
      "type": "array",
      "items": { "type": "string" }
    },
    "message": { "type": "string" }
  },
  "required": ["path", "count", "pages", "message"]
}
